#ifndef RANDOMFUNCTIONS_H
#define RANDOMFUNCTIONS_H

/**
* Returns a random number from interval [l,r].
*
* @param l left bound of interval a random number needed from
* @param r right bound of interval a random number needed from
* @return a random number from an interval from l to r
*/
int get_rand_from_range_incl(int l, int r);

#endif // RANDOMFUNCTIONS_H
